t=int(input())
for w in range(t):
    l=input().split(" ")
    d=abs(int(l[0]))
    p=abs(int(l[1]))
    c=0
    for i in range (-p,p+1):
        for j in range(-p,p+1):
            if(abs(j-i)==d and abs(j*i)==p):
                c+=1
    print(c)
